
<% include ../partials/header.ejs %>






<header class="jumbotron jumbotron-fluid" id="old_jumbotron">
<div class="container" id="roo">
<div id="foo">
<h1>Welcome to <span id="hoo"><i class="fa fa-lightbulb-o" aria-hidden="true"></i>CampPrep</h1></span>
<p><b>A place to share and explore success paths , ideas ,career talk and many more..</b></p>
<p>
</div>
<a class="btn btn-success btn-large"  href="/campgrounds/new">Add a new CampPrep</a>
</p>
</div>

</header>
<div class="container">
<div class="container ">

<div class="row text-center container-fluid" >
 <% campgrounds.forEach(function(campground){ %>
<div class="col-md-3 col-sm-6">

	<div class="thumbnail" >
<img src="<%= campground.image %>" style="width:100%;height:250px;%"" >

<div class="caption">
<% if((campground.name).length>16) {%>
<h4><%= (campground.name).slice(0,16)+"..." %></h4>
<% }else{ %>
<h4><%= campground.name %></h4>
<% } %>
</div>
<p><a href="/campgrounds/<%= campground._id %>" class="btn btn-primary">More info</a>
</p>
</div>

</div>
<% }); %>

</div>

</div>
   <% include ../partials/footer.ejs %>
